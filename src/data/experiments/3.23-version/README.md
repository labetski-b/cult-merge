# 3.23 Version Balance Backup

Frozen snapshot of the current production balance profile before the Kraken Level XP rebalance experiment.

Source of truth at backup time:

- `src/data/*.json`

This folder is a backup/reference snapshot only. Production balance remains in `src/data/*.json`.
